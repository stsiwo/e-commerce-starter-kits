declare module 'react-spring'

/**
 * NOTE: current react-spring@v9.0.0-rc.3 does not have proper type definition. well, it actual has type definition but the implementation is terrible so stop using the type definition instead ignore the type
 **/
